import { useTranslation } from "react-i18next";
import { Dialog, DialogContent, DialogDescription, DialogHeader, DialogTitle } from "@/components/ui/dialog";
import { SteamWorkshopBrowser } from "./SteamWorkshopBrowser";
interface Props { open:boolean; onOpenChange:(open:boolean)=>void; onInstalled:(mods:any[])=>void; mode?:"wishlist"|"install"; }
export function WorkshopInstallDialog({open,onOpenChange,onInstalled,mode="wishlist"}:Props){const {t}=useTranslation(); const conan=mode==="install"; return <Dialog open={open} onOpenChange={onOpenChange}><DialogContent className="max-w-3xl"><DialogHeader><DialogTitle>{conan ? "Browse Conan Exiles Steam Workshop" : t("mods.steamBrowser.title", {defaultValue:"Browse Steam Workshop"})}</DialogTitle><DialogDescription>{conan ? "Search the Conan Exiles Workshop and install mods directly through SteamCMD for the selected Conan server." : t("mods.steamBrowser.description", {defaultValue:"Search Palworld Workshop mods and add any of them to the server wishlist for the super admin to review."})}</DialogDescription></DialogHeader><SteamWorkshopBrowser mode={mode} onInstalled={onInstalled}/></DialogContent></Dialog>}
